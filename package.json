{
  "name": "campaign-pipeline",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "setup": "npm run install:all && echo '\\n✅ Done! Copy backend/.env.example to backend/.env and add your OPENAI_API_KEY'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "description": "A full-stack proof-of-concept that automates creative asset generation for localized social ad campaigns using GenAI.",
  "main": "index.js",
  "author": "",
  "license": "ISC"
}
